{{- with index .Values "root-ingress" -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
  {{- if .appRoot }}
    nginx.ingress.kubernetes.io/app-root: /{{ .appRoot }}
  {{- end }}
    kubernetes.io/ingress.class: nginx
  {{- end }}
    nginx.ingress.kubernetes.io/proxy-body-size: 150m
  {{- if ne $.Values.global.setup "quickstart" }}     
    #certmanager.k8s.io/cluster-issuer: {{ index . "cert-issuer" }}
    cert-manager.io/cluster-issuer: {{ index . "cert-issuer" }}
  {{- end }}  
  name: {{ .Values.rootingress.name }}
  namespace: {{ .Values.rootingress.namespace }}
spec:
  rules:
  - host: {{ $.Values.global.domain }}
    http:
      paths:
      - backend:
          service:
            name: {{ .Values.rootingress.serviceName }}
            port: 
              number: {{ .Values.rootingress.servicePort }}
        path: /
        pathType: Prefix
{{- if ne $.Values.global.setup "quickstart" }}        
  tls:
  - hosts:
    - {{ $.Values.global.domain }}
    secretName: {{ $.Values.global.domain }}-tls-certs    
{{- end }}         
